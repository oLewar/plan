# Raw Inbox

`raw/` — только входящая свалка.

- Без вложенных папок.
- Новые материалы складываются только в корень `raw/`.
- Ingest по cron раскладывает материалы по тематическим разделам вне `raw/`.
- Внешние картинки локализуются в `assets/external/` с перелинковкой.
